import React from "react";
import DocCard from "./DocCard";

const Response = () => {
   return (
      <div className="bg-br rounded p-2 w-full md:w-1/2">
         <p>
            200 A successful request returns the HTTP 200 OK status code and a
            JSON response body that lists transactions .
         </p>
      </div>
   );
};

export default Response;
